# --- Core framework ---
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6

# --- Persistence ---
SQLAlchemy>=2.0
psycopg[binary]>=3.2        # PostgreSQL driver (prod)
alembic>=1.14

# --- Cache / broker ---
redis>=5.2                  # Redis cache + rate limiting
celery>=5.4                 # Smart-Sync queue worker (RabbitMQ/Redis broker)

# --- Security ---
PyJWT>=2.9                  # JWT device + user tokens
passlib[bcrypt]>=1.7        # password hashing
python-multipart>=0.0.12    # OAuth2 form login

# --- AI / reports ---
google-generativeai>=0.8
reportlab>=4.2              # SLA violation PDF reports
httpx>=0.28
